

class UserAvater extends StatelessWidget {
  final double size;
  const UserAvater({super.key, this.size = 40});

  @override
  Widget build(BuildContext context) {
    return ClipRRect(
      borderRadius: BorderRadius.all(Radius.circular(16)),
      child: Image.asset(
        AppIcons.user_Avater,
        height: size,
        width: size,
      ),
    );
  }
}
